//El método includes determina si un array o string incluye un determinado elemento. 
//Devuelve true o false, si existe o no respectivamente.

let numbers = [1, 2, 3, 4, 5, 6, 8]
console.log(numbers.includes(9))  

const list = ['Oscar', 'David', 'Ana'];
console.log(list.includes('oscar')) //devuelve false porque aunque lo incluye, no tiene al mayúscula