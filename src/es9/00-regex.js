//expresiones regulares o Regex

const regex = /(\d{4})-(\d{2})-(\d{2})/;
const matchers = regex.exec('2022-01-01');
console.table(matchers);  //imprime en tabla



















let arr = [1, 2,3]
arr[10] = 'hello'

console.log(arr.length)